bool isPowerOfThree(int n) {
    long long int expo = 1;
    while(n >= expo)
    {
        if(n == expo)
        {
            return true;
        }
        else
        {
            expo *= 3;
        }
    }
    return false;
}
